# evidence behind your notes audit

Joel, this is the detail behind the shorter audit. it records what the code supports, what the live database proved and what still needs a real run with you

## fixes made during this audit

- Applied `20260905210500_show_ops_membership_policy_recursion.sql` to Solvio. The authenticated owner business lookup reproduced error 42P17 before the fix and returned the correct workspace after it. A direct membership read also succeeds; this workspace currently returns zero membership rows for the owner. The owner account itself is separate from membership rows. No live seller login was invented for this test
- Applied `20260905211000_show_ops_booking_policy_performance.sql`. Staff business membership and island scope now evaluate once per statement. Sellers still do not gain direct staff access to bookings. Owner scope wins over a restricted membership
- The same authenticated year-of-partner-totals query measured 699.156 ms before, 273.746 ms first run after and 19.784 ms on repeat. All returned 225 partner groups from 11,090 qualifying bookings. These are individual database timings and not a controlled whole-browser benchmark
- Reports now reads every page until empty, detects later-page errors, and refuses a dataset above 100,000 rows with a narrower-period message. It loads comparisons in parallel and selects fewer booking fields. Mixed-currency configured workspaces require one island for monetary totals; invoice/payment filters follow it. Legacy invoices without an island are explicitly excluded by that filter
- Render-time workspace loading is reused within one request. Mutating actions retain fresh checks
- Review caught and corrected leap-day prior-year dates so a February 29 report does not fail on February 29 of a non-leap year

## remaining limits after those fixes

Reports still assembles rows in server memory and separate pages are not one immutable database snapshot. We need a live full-page timing and a reconciliation against your workbook. The report label Net sales currently means gross sales after no-show write-offs, not partner nett; the partner-type filter does not apply to every comparison/invoice/payment tile. Currency configuration must cover historical islands correctly. These require clearer definitions and further work

The pagination repair applies to Reports, not Dashboard, Outlook, CSV exports, the daily digest or invoice preview/generation. The initial findings below about those other routes remain outstanding. The Reports row-limit and swallowed-error findings below describe the starting state and are corrected in this release

## live data checked

The intended workspace in Supabase `aasfahcrdcoqxwnlkdnv` contains 16,032 bookings, 1,213 hotels, 327 pickups, 418 timetables, 469 partners, 14 shows, 39 rate cards and 9,608 rate prices. Zero partners explicitly include Gran Canaria in their island tag; ALL/shared tags are a different case. The transport supplement is 10. Booking dates span 2025-09-05 through 2027-10-24. Exact missing-nett counts and intended contractual rates were not reconciled. Stored legacy rate-card rows are not proof the active pricing calculator uses them

## initial code findings by area

These findings were captured against commit `8ffa121` before the fixes above. Source line numbers refer to that inspected code and can move after the Reports changes. “Supported code” is not live acceptance. For a current customer-facing status use [your notes audit](./MHT%20-%20your%20notes%20audit%20-%205%20September.md). No real external messages, financial transactions, guest-booking edits, backup restores or live show-night trials were performed

## General Hub notes

| Your note / claim in the supplied document | Verdict | What I can honestly tell you now | Evidence and remaining check |
|---|---|---|---|
| “Done means it is on the hub right now”; everything described as live today (source lines 1–17) | **partial** | The current implementation supports many of these items. The label “live” still needs a release match and live acceptance for each important flow. No real show-night acceptance is established by this audit. | Current checkout is `8ffa121`; no fresh production release/session verification performed. The old ledger explicitly distinguishes implementation from live acceptance: `docs/reviews/2026-09-05-mht-joel-evidence-ledger.md:7-9`. |
| “Very slow?” — fixed, formerly a 24 MB page; now only loads what you are looking at (19–26) | **partial** | The directory now loads the relevant data and displays manageable chunks, and the search can find hotel names beyond the first database page. That does not prove the whole hub is fast or that this particular old 24 MB measurement was accurate. We still need timings for the slow page you actually use. | `src/app/dashboard/show-ops/master/page.tsx` has `wantsMaster`/tab-dependent queries; `src/lib/show-ops/directory-data.ts` paginates all hotel/pickup records; `src/components/show-ops/hotels-directory.tsx:29` sets page size 50. Paginated **database** reads still assemble the full accessible directory for client search; they do not mean only 50 hotels are fetched. The measurements above now cover the database fault. |
| App/mobile version, home-screen installation (27–33) | **supported code** | There is a responsive web app and an installable-style manifest. I would describe that as the mobile web version, not an App Store release or a guaranteed offline app. We should still walk through the important screens on your phone. | `src/app/manifest.ts:5-18` sets standalone display, icons and start URL `/`; responsive classes appear throughout `src/app/partner/layout.tsx` and staff pages. No device-by-device acceptance or App Store distribution evidence in this audit. |
| Link to Jesús for Verifactu (34–44) | **needs input** | The invoice settings and an outbound provider adapter exist. Jesús still needs to confirm the required legal details, provider and certificate arrangement. This is not evidence of a certified, approved or live tax connection. | `src/lib/show-ops/verifactu.ts:8-29` checks configuration and returns manual mode when no API key is set; configured flow posts a payload at lines 34–53. `src/app/dashboard/show-ops/settings/page.tsx:896-936` contains tax/issuer fields. No provider configuration, submission or approval checked live. |
| Link/introduction to Vince (45–48) | **needs input** | We need the actual systems and account owner/contact details from Vince before promising how email, messaging, marketing and the website connect. | These are external dependencies, not something a code audit can confirm. No introduction or external communication was sent. |
| Email/SMS/WhatsApp/MailerLite/GDPR/merging/direct website integration, “one switch” makes everything live (49–58) | **partial** | Guest email and phone-message sending exist, with deliberate delivery gates. Turning on the gate is only one prerequisite: sender/provider configuration and a controlled delivery test are also needed. MailerLite, GDPR processes, data merging and a direct website integration are separate unresolved items; a shared list of names is not a completed integration. | `src/lib/show-ops/outbound.ts:3-39` permits allowlisted email recipients in test mode and all valid requested recipients only when the live flag is enabled. `src/lib/notifications/show-ops-guest-ticket.ts:212-243` requires live mode for phone messages and uses configured WhatsApp with SMS fallback. `src/lib/notifications/booking-whatsapp.ts:32` reports missing provider configuration. No MailerLite route/module was found in the targeted route search. **The production flag and actual delivery were not checked.** |
| GYG API, full two-way integration needs approval/timing (59–68, repeated 75–83) | **needs input** | GYG can be represented as a sales channel/partner. This audit has not established a connected GYG API. The exact API access, scope, cost and approval timetable need confirming; “takes weeks” is not a verified commitment. | `src/lib/show-ops/config.ts:257` includes GYG as a configured sales channel. No GYG-specific app route was found in the targeted route search. Absence in this targeted search is not a whole-repository proof of absence. Commercial package labels are not technical or client-acceptance evidence. |
| Backup/server: automatic full copy every five minutes, last hour/all + hourly/day + daily/month, paid daily database backup and a restore drill (69–93) | **partial — “full copy” is false for the current export** | The configured schedule is every five minutes, with the stated tiered retention. However, the custom export is not a full recoverable copy of everything now in the hub: its table list omits newer data, and it does not package the original ticket photo files or Auth accounts. We also need a successful recent run and a restore drill before claiming recovery is ready. | `vercel.json:10-12` schedules `*/5`; `src/lib/show-ops/backup.ts:95-101` defines 60-minute/all, 24-hour/hourly and 30-day/daily retention; pruning implementation at 112 onward. Backup table allowlist at `backup.ts:14-25` omits `show_ticket_types`, `show_extras`, `show_bus_night_orders`, rate tables and booking audit/history tables. Export function at 59–72 serializes business/table rows, not storage bytes or Auth accounts. `src/app/dashboard/show-ops/backup/page.tsx:39-40,59` still incorrectly says **six hours**, contradicting the configured five-minute cadence. Supabase paid-plan backup entitlement/status and a restore drill were not verified. |
| Monthly reports to chosen users/emails/download, daily digest already built, “every report” CSV (94–103) | **partial / needs input** | A daily digest and sample-send control exist. Scheduled monthly report packs, recipient rules and Google Sheets export are not established by this audit. We need to agree who gets which reports and then verify that exact workflow. “Every report downloads” is too broad without checking each report. | `vercel.json:6-8` schedules the daily endpoint at 07:00 UTC; `src/app/dashboard/show-ops/actions-reports.ts:25-49` checks global admin and the email gate, then builds the digest. `src/app/dashboard/show-ops/settings/page.tsx:526-563` contains daily-recipient settings. Schedule is UTC; do not silently describe that as 07:00 local year-round. No monthly cron appears in `vercel.json`. |

## Seller link

| Your note / claim (source lines 165–191) | Verdict | What I can honestly tell you now | Evidence and remaining check |
|---|---|---|---|
| Show me the partner link: invite by email, own bookings only, cannot overbook | **supported code; live delivery unverified** | The invitation now uses a secure one-time link. An ordinary seller sees their own bookings/results; a crowned organisation administrator can manage ordinary sellers and see their organisation’s results within their island access. That admin distinction should be explained in the demonstration. Blocked or failed email does not report success; resending does not replace an existing password. | `src/lib/show-ops/partner-invitations.ts` orchestrates delivery gating, identity/membership checks, fresh authority checks, token-hash links and retryable failure. `src/app/dashboard/show-ops/actions.ts:2011-2056` checks fresh authority and inserts a scoped membership. `src/lib/show-ops/access.ts:316-325` rechecks organisation-admin status. `supabase/migrations/20260905180237_show_ops_partner_organisations.sql:59-80` stamps creator identity and limits returned rows. `src/app/partner/page.tsx:119,210,286` distinguishes self/organisation views. No recipient inbox or live Auth flow was exercised. |
| Cancellation request only 24 hours before, approved by Ruth/us | **needs input; workflow not implemented in the current portal** | This is not a completed seller cancellation workflow. The portal tells sellers to contact the office. We need a precise cutoff and approval process before calling it built. Any previous “building this month” date needs a fresh commitment, not an automatic rollover. | `src/app/partner/page.tsx:290` says to contact the office for cancellations. Current partner routes contain no cancellation-request page. Clarify whether cutoff is 24 hours before show start, and whether requests are permitted up to that cutoff or only inside a 24-hour window. |
| Sellers see invoices | **needs input / not implemented in inspected portal routes** | The note correctly says seller invoice viewing is not there. Existing staff invoicing is not the same as seller document access. Its commercial classification also needs checking against the earlier promise that it was done. | Current `src/app/partner/` routes cover overview, new booking, team, password and ticket-photo pages; no seller invoice route. `docs/reviews/2026-09-05-mht-joel-evidence-ledger.md:36` records the earlier contradictory promise and says it must be reconciled. |
| Sellers see ticket pictures; note says this is not yet available | **false / outdated** | Sellers can now attach and view the original ticket photo on their **own booking**. That does not give them a general invoice-photo archive or visibility of other sellers’ photos. | `src/app/partner/tickets/[bookingId]/page.tsx` reads the own-booking RPC and creates a short-lived private photo link; `src/app/partner/tickets/[bookingId]/actions.ts` validates and uploads through the authenticated client. `src/lib/show-ops/partner-ticket-photo.ts` applies ownership and file validation. `src/lib/show-ops/invoice-photos.ts:27-70` validates original files for invoice attachment. Live upload/download acceptance not performed in this audit. |

## Partners

| Your note / claim (source lines 261–303; duplicate ending 304–310) | Verdict | What I can honestly tell you now | Evidence and remaining check |
|---|---|---|---|
| Only owners/super admins | **supported code with clarified roles** | Owner/admin roles can manage partner catalogue records; restricted admins are limited by island scope. Workspace-wide roles and island permissions require the owner or an administrator with access to all islands. “Super admin” should not be used as though it is a separate existing role. | `src/app/dashboard/show-ops/actions.ts:470,670,1481` use admin-floor catalogue guards; `src/lib/show-ops/access.ts:329-336` handles unrestricted administrative actions. `supabase/migrations/20260905183006_show_ops_island_permissions.sql:3-24` defines owner/global admin, island and catalogue-scope checks. |
| Island sort/filter/search; multi-island partners; “nobody tagged Gran Canaria yet” | **partial** | Island filtering, searching and multi-island partner tagging exist. The claim that no partner is tagged to Gran Canaria is a data snapshot, not a lasting fact, and needs a fresh query before repeating it. | `src/components/show-ops/master-suppliers-form.tsx` includes island filtering and bulk partner updates; bulk controls at 235–296. `src/lib/show-ops/partners.ts` handles partner-island normalization. No current production tag counts were queried. |
| Delete option automatically switches a used partner off | **false as worded** | An unused partner can be deleted. A partner with bookings or invoices is **not automatically archived** by Delete: the action refuses and tells you to untick Active instead. The answer also incorrectly refers to deleting a “show” under the Partners heading. | `src/app/dashboard/show-ops/actions.ts:1480-1496` counts bookings/invoices, returns “untick Active instead of deleting”, then deletes only if unused. No deletion was attempted. |
| Invoice nett 100%, 236 imported missing rates; 100% means no commission | **supported code for the rate meaning; current counts need verification** | The current calculation uses the partner’s invoice nett percentage: 100% retains the full ticket gross. The claim about exactly 236 missing rates is unverified historical data. We need Ruth’s intended rates and a current reconciliation before changing anything. | `src/lib/show-ops/calc.ts:88-96` derives adult/child/infant nett from the supplier percentage and adds extras separately. Non-commissionable extras retain their full value via `src/lib/show-ops/extras.ts`. `src/components/show-ops/master-suppliers-form.tsx:243-247,287-296` supports bulk percentage changes. No current production count or rate correctness checked. |
| Invoice rates/settings and “39 cards, 9,608 prices imported” | **partial** | Partner deposit and invoice settings are present. Imported legacy rate-card counts are not verified here, and their existence would not prove that booking pricing uses those imported cards. The current booking calculation uses show/ticket prices and the chosen partner percentage. | `src/components/show-ops/master-suppliers-form.tsx:438-439` passes separate billing settings; `src/app/dashboard/show-ops/actions.ts` builds booking money from the selected show/ticket and supplier. Targeted action search found no current booking lookup of `show_rate_prices`; its presence in product deletion at 1473 is not pricing integration. Reconcile legacy-specific contracts before promising them. |
| Deposit or take all, configurable by partner/desk override | **supported code** | Each partner has a deposit/invoice mode and percentage; a 100% deposit means collect the full amount. The desk can choose billing mode only where the partner explicitly allows that. This should be demonstrated with a real agreed partner configuration. | `src/components/show-ops/master-suppliers-form.tsx:29-38,438-439`; server `buildBookingFields` in `src/app/dashboard/show-ops/actions.ts` checks `can_choose_billing_mode`; `src/lib/show-ops/calc.ts` computes deposit from total gross. Actual intended rates still need your and Ruth’s confirmation. |
| Booking supplier link demonstration | **supported code; demonstration outstanding** | The authenticated partner portal exists, with ordinary seller and organisation-admin accounts. A demo is still needed; this audit did not send an invitation or log in as a live seller. | Same invitation/portal evidence as Seller link above. The invitation is an identity-bound login, not a public referral/booking bearer link. |
| Cancellation requests plus no-overbooking both marked Done | **partial** | The capacity guard exists, including serialisation around the last seat. Cancellation requests remain separate and are not completed by having that capacity guard. These must be two rows with two statuses. | `supabase/migrations/20260905155752_show_ops_partner_safety.sql:31-115` takes a transaction advisory lock and raises show/bus-full errors; staff capacity-changing writes share the lock. `src/app/partner/page.tsx:290` still directs cancellation enquiries to the office. Database code is not a fresh proof of the currently deployed migration or every real-night capacity setting. |

## Settings

| Your note / claim (source lines 541–573; repeated page-break rows consolidated) | Verdict | What I can honestly tell you now | Evidence and remaining check |
|---|---|---|---|
| Roles and page permissions, senior-only catalogue | **supported code; setup with your team still needed** | Role and page permissions exist, and island access now adds a separate boundary. Owner/unrestricted admin controls global permissions; a restricted admin can work only within their permitted islands. We still need the actual matrix for Joel, Ruth, Rich and each desk user. | `src/lib/show-ops/access.ts:303-336`, `src/lib/show-ops/island-access.ts`, `src/components/show-ops/member-islands-form.tsx`, and `show_ops_island_permissions.sql`. `src/app/dashboard/show-ops/actions.ts:2263-2270` no longer replaces a password when “create staff” meets an existing account. |
| In-house mail sample “lands in your inbox once cleared” | **partial** | The button exists and observes the test-recipient gate. Clearance alone does not guarantee arrival: provider configuration, acceptance and inbox delivery still need testing. It is currently a global-admin action, not a sample available to every restricted user. | `src/app/dashboard/show-ops/actions-reports.ts:25-49` requires global admin, checks the gate and builds the digest. Provider result handling later in the same action distinguishes error from submission. No sample was sent here. |
| Verifactu name/details settings | **supported code; legal/provider acceptance needs input** | Legal issuer name, NIF, address, series and tax fields can be configured. Jesús needs to confirm the values and provider workflow. A configured form is not certification. | `src/app/dashboard/show-ops/settings/page.tsx:896-936`; `src/lib/show-ops/verifactu.ts:8-29`. |
| Backup options | **partial** | The page and JSON export are available to global admins. The schedule text is inconsistent and the exported table set is not yet a full recovery package; the General Hub backup correction applies here too. | `src/app/dashboard/show-ops/backup/page.tsx:20-24,39-40,81-87`; `src/app/api/show-ops/export/route.ts` rejects non-global access; `src/lib/show-ops/backup.ts:14-25`. |
| “All real data now”: 1,213 hotels, 327 pickups, 418 timetables, 469 partners, 14 shows, 39 rate cards, about 16,000 bookings/full year | **needs verification** | Those exact counts, source completeness and future-month coverage must be checked against the right live business and import reconciliation. The pages using stored data is not proof that every row was imported once, classified correctly or priced correctly. Do not reuse these numbers from the document as current facts. | The current directory loaders (`src/lib/show-ops/directory-data.ts`) and partner analytics pagination prevent one known 1,000-row truncation problem. No production dataset/count comparison or import reconciliation was performed in this audit. The earlier evidence ledger is historical and cannot settle today’s counts. |

## Corrections to prioritise before reissuing the answers

1. Replace blanket “Done = live” with per-item implementation and live-acceptance evidence.
2. Separate seller cancellation requests from the implemented capacity guard.
3. Update the seller ticket-photo answer: own-booking upload/view exists now; invoice access remains a different unfinished feature.
4. Correct partner Delete wording and recheck imported rate/count claims rather than carrying forward old numbers.
5. Correct backup page cadence, expand/review the actual recovery set, confirm recent successful runs and complete the restore drill. The custom backup is not currently a full substitute for a database/storage/Auth recovery plan.
6. Test one authorised email recipient and provider configuration before promising an invitation or sample “lands”. No message has been sent as part of this audit.
7. Keep commercial scope and prices separate from these technical findings. A “Needs input” verdict is not permission to reprice an earlier promise.


## Material corrections before calling the notes done

- “Every booking freezes prices” is too broad. Invoice generation explicitly reprices imported bookings without a pricing snapshot from current master data (`src/app/dashboard/show-ops/actions.ts:2552`). Snapshot-bearing desk bookings do retain their sold rates.
- “UK pounds automatically through tickets and payment links” is not end-to-end correct. Payment-link loading omits island (`src/app/dashboard/show-ops/actions.ts:2444`), then chooses currency from that missing field (`:2480`, `:2493`). Background ticket sending also omits island and uses default config currency (`src/lib/notifications/show-ops-guest-ticket.ts:257`, `:270`, `:292`).
- Configured show start time is displayed in the booking form, but the public guest ticket still derives Morning/Evening from AM/PM (`src/app/ticket/[token]/page.tsx:54`, `:88`), as does background ticket sending (`src/lib/notifications/show-ops-guest-ticket.ts:295`). A claim that guests receive “7pm doors” is not supported end-to-end.
- The bus supplement is one configurable amount shared by adults and children, not an independently configurable child bus charge (`src/lib/show-ops/calc.ts:65`, `:75`; `src/app/dashboard/show-ops/settings/page.tsx:748`). Setting the child ticket price to zero still adds the supplement when bus transport is selected.
- “Every edit” needs a date caveat: automatic history begins 5 September; older missing audit records are not reconstructed (`supabase/migrations/20260905184331_show_ops_booking_audit.sql:1`; booking detail notice `src/app/dashboard/show-ops/bookings/[id]/page.tsx:398`).
- Email and text sending are implemented, including WhatsApp-first/SMS fallback, but configuration, live gate state and actual delivery have not been verified. No recipient was contacted in this audit.

## Shows — source text lines 192–259

| Source note / associated answer | Verdict | Evidence and limits |
|---|---|---|
| Only owners / super admin, e.g. Ruth; database enforced | supported-code | Catalogue restrictive policies use the senior guard: `supabase/migrations/20260905155752_show_ops_partner_safety.sql:6`, `:23`. Product actions require admin (`src/app/dashboard/show-ops/actions.ts:514`, `:599`). Actual role is owner/admin, with island scope restrictions; Ruth's membership was not checked. |
| Confused by options; one sitting explains show, island, types, transport prices, days, start time, capacity | partial | Editor groups physical show and shared capacity (`src/components/show-ops/master-shows-form.tsx:463`, `:467`), start time (`:472`), run days (`:498`), base prices (`:528`) and bus supplement (`:559`). UI now has single ticket price plus supplement, so “prices with and without transport” is stale. A walkthrough has not been accepted. |
| Delete option: unused delete, booked show switch off | supported-code | Delete checks existing bookings (`src/app/dashboard/show-ops/actions.ts:1460`, `:1466`); editor supplies archive via Active and preserves history (`src/components/show-ops/master-shows-form.tsx:280`, `:291`, `:355`). |
| Changes must preserve earlier bookings and invoicing; every booking freezes prices; push change to “unbooked ones” | partial | New bookings store snapshot (`src/app/dashboard/show-ops/actions.ts:1771`); note-only edits preserve it (`:1885`, `:1916`). Invoice honours snapshots but explicitly reprices legacy rows without snapshots (`:2552`). Reprice button targets existing uninvoiced, noncancelled, nonlegacy bookings (`:860`), not “unbooked ones”; it includes past/future bookings, as explained in `src/components/show-ops/master-shows-form.tsx:328`. |
| Schedule price changes for forward bookings | needs-input | No effective-date scheduling UI; reprice copy explicitly distinguishes current prices and existing bookings (`src/components/show-ops/master-shows-form.tsx:328`). Source correctly leaves scope open. |
| Netts not needed on show, only partners | supported-code | Current price editor explains partner nett at `src/components/show-ops/master-shows-form.tsx:559`; calculation ignores legacy product nett fields and uses partner percentage (`src/lib/show-ops/calc.ts:44`). |
| Why two price lines? Fixed €10 adult and child; child can be zero | partial | One base price per age plus configured bus supplement (`src/components/show-ops/master-shows-form.tsx:537`, `:544`, `:551`, `:559`). MHT default supplement is 10 (`src/lib/show-ops/config.ts:266`), but live configured value not checked. Child base price may be zero; there is no independent zero-child-bus-supplement switch (`src/lib/show-ops/calc.ts:65`, `:75`). Currency is island-dependent, not universally euros. |
| Transport nights and resorts; only stops running that night | supported-code | Stop date eligibility `src/lib/show-ops/bus.ts:38`, `:54`; show weekdays `src/lib/show-ops/nights.ts:39`. Existing selected stop can remain visible when editing. Configured night/resort values not independently verified. |
| GBP for UK, EUR islands; automatic tickets, invoices, payment links | partial | Currency helper supports UK/overrides (`src/lib/show-ops/config.ts:67`), invoice selects pack island (`src/app/dashboard/show-ops/actions.ts:2618`). Payment-link SELECT omits island (`:2444`) despite currency lookup (`:2480`, `:2493`); background guest ticket uses default currency (`src/lib/notifications/show-ops-guest-ticket.ts:270`). UK end-to-end trial outstanding. |
| Ticket type with its own price, more custom if wanted | supported-code | Parent editor embeds ticket types and extras (`src/components/show-ops/master-shows-form.tsx:301`); same physical show's types share capacity (`:463`). Custom type editor is `src/components/show-ops/ticket-types-editor.tsx`; optional extras editor is `src/components/show-ops/extras-editor.tsx`. Source “in progress” status is stale relative to code. |
| Real rate-card prices loaded; all start times 7pm doors | needs-input | Form supports real values (`src/components/show-ops/master-shows-form.tsx:472`, `:537`), but source-rate-card accuracy and live data cannot be inferred from UI. Guest ticket still uses AM/PM wording (`src/app/ticket/[token]/page.tsx:54`, `:88`). Require authoritative rates/start times plus data comparison. |

## Hotel pick-ups — source lines 311–323

| Source note / answer | Verdict | Evidence and limits |
|---|---|---|
| Owners / super admin only | supported-code | Hotels/stops included in senior database policies (`supabase/migrations/20260905155752_show_ops_partner_safety.sql:23`); scope restrictions also apply. |
| Buffering fixed, loads instantly | partial | Directory uses compact paginated display in place of many edit forms (`src/components/show-ops/hotels-directory.tsx:38`). Full authenticated directory loaders paginate query results (`src/lib/show-ops/directory-data.ts`). “Instantly” requires measured browser/load testing; not established by source inspection. |
| Search, island/resort filters; hotel names plus pickup/time | supported-code | Directory filtering and controls `src/components/show-ops/hotels-directory.tsx:79`, `:110`; hotel display `:293`. Missing configured pickup/time data can still display empty values. |
| Separate hotel and pickup tabs | supported-code | `src/app/dashboard/show-ops/master/page.tsx:438`, `:444` renders distinct Hotels and Pick-up points tabs; hotel rows at `src/components/show-ops/hotels-directory.tsx:293`. |
| Guide notes picture/link and printable/download bus list | supported-code | Stop guide notes/map/photo URL fields `src/components/show-ops/pickup-points-directory.tsx:379`, `:386`, `:397`. Bus print/PDF/CSV controls `src/components/show-ops/bus-run-sheet.tsx:317`; notes and safe photo/map links `:378`. Browser print includes external photo image; downloaded PDF contains photo/map links, not fetched embedded remote pictures. Actual photo URL availability remains dependent on entered data. |

## Bus board — source lines 325–349

| Source note / answer | Verdict | Evidence and limits |
|---|---|---|
| Permanent vs one-night stop ordering, save/reset/share | supported-code | Permanent order `src/app/dashboard/show-ops/buses/page.tsx:253`; nightly saved order/drag/save/reset `src/components/show-ops/bus-run-sheet.tsx:96`, `:150`, `:174`, `:200`. Print/PDF/CSV `:317`; share uses saved ordering, requiring save before sharing. Cross-phone persistence is implemented, not live trialled. |
| Guide name per bus order, on printed island list | supported-code | Guide field `src/app/dashboard/show-ops/buses/page.tsx:213`, guide display `:166`; PDF data/renderer `src/lib/show-ops/bus-pdf-data.ts`, `src/lib/show-ops/bus-pdf.ts`. Real assignment values not verified. |
| Email bus list to guide “being added this month” | supported-code | Already implemented: `src/app/dashboard/show-ops/actions-bus-send.ts:10`, `:41` sends PDF attachment through gated email. This source future-tense status is stale. No real send/delivery trial. |
| SMS/WA with stop photo/map/read confirmation or autoreply | needs-input | Existing guest messages carry ticket link/stop/time (`src/lib/notifications/show-ops-guest-ticket.ts:112`, `:214`), with WhatsApp/SMS transport. No demonstrated specialised stop-image/map acknowledgement/read-receipt workflow; source correctly leaves richer flow and Vince integration/costs to agree. |

## Bookings — source lines 351–377

| Source note / answer | Verdict | Evidence and limits |
|---|---|---|
| Attach ticket when all IN for invoice; supplier number/photo | partial | Supplier number stored `src/app/dashboard/show-ops/actions.ts:1763`; booking form field `src/components/show-ops/booking-form.tsx:1690`; door photo control `src/app/dashboard/show-ops/door/page.tsx:258`. Invoice evidence attachment service exists (`src/lib/show-ops/invoice-delivery.ts:37`), so “being built” is stale. Exact custom IN stamp/automatic attach-on-all-in requirement remains unspecified. |
| Partner type automatically sets channel, override allowed | supported-code | Partner selection updates channel `src/components/show-ops/booking-form.tsx:467`; override field `:1240`. Actual partner classifications still require data review. |
| Creator/time/date and every edit, before/after, cancellations | partial | Creator display `src/app/dashboard/show-ops/bookings/[id]/page.tsx:140`, `:177`; row audit trigger `supabase/migrations/20260905184331_show_ops_booking_audit.sql:34`; payment audit `:51`. Audit excludes token-only/technical updates and starts 5 September; older missing history explicitly disclosed at booking page `:398`. Cannot claim complete retrospective history. |
| Cancel without removing, retain who/why, filter cancelled | supported-code | Cancel action `src/app/dashboard/show-ops/actions.ts:1951`; cancelled display `src/app/dashboard/show-ops/bookings/[id]/page.tsx:189`; index cancelled filter `src/app/dashboard/show-ops/bookings/page.tsx:94`, `:386`. Invoiced bookings have additional cancellation guards. |

## Night lists and Door clarification — source lines 379–410

| Source note / answer | Verdict | Evidence and limits |
|---|---|---|
| Quick check-in renamed Night lists, printed office/bus/meals vs Door | supported-code | Lists route `src/app/dashboard/show-ops/lists/page.tsx:400`; explicit Door distinction `src/app/dashboard/show-ops/door/page.tsx:119`; Door scanner `:123`. Both can update arrivals; names distinguish intended use, not separate booking records. |
| All in or number arrived; missing charge/write-off under partner rule | supported-code | Partial count bounded to party size, All in and Undo `src/components/show-ops/arrival-pax-form.tsx:27`, `:44`, `:63`; decision UI `src/components/show-ops/no-show-decision.tsx`. Actual partner policies require checking. |
| Hide cash/card on invoice bookings | supported-code | Lists `src/app/dashboard/show-ops/lists/page.tsx:1081`, `:1296`; Door `src/app/dashboard/show-ops/door/page.tsx:197`, `:260`. |
| Print every list with logo; layout | partial | Shared print branding header `src/app/dashboard/show-ops/lists/page.tsx:526`; bus separate print/PDF controls `src/components/show-ops/bus-run-sheet.tsx:317`. Code supports printing, but long real names/notes, page breaks and every output layout not visually accepted in this audit. Bus PDF download/email now exists; not a universal PDF/email facility for every list. |
| Daily sales moved to reports; date/island/channel/hour/CSV | supported-code | `src/app/dashboard/show-ops/reports/page.tsx:818`, `:842`, `:862`. Cancellation exclusion explained at `:820`; source data accuracy not independently reconciled. |
| Door is not the printed office list | supported-code | Door purpose text `src/app/dashboard/show-ops/door/page.tsx:119`; scanner/arrival controls `:123`, `:247`; printable lists header `src/app/dashboard/show-ops/lists/page.tsx:526`. |
| Diet amber, owed red, comments purple; desktop dots/phone chips/print text; no sideways scrolling | partial | Colours/dots/full print text `src/components/show-ops/booking-flags.tsx:29`, `:40`, `:54`; phone chips `:95`; list fixed layout `src/app/dashboard/show-ops/lists/page.tsx:883`. Colour behaviour supported. Universal “whole sheet fits one screen” not established for all widths and long content. |

## Door — source lines 412–443

| Source note / answer | Verdict | Evidence and limits |
|---|---|---|
| Saving sends guest QR ticket email/WhatsApp; scan marks arrival; repeated scan already in; real sends off | partial | Creation invokes ticket sender `src/app/dashboard/show-ops/actions.ts:1806`; QR lookup/cancel/repeat handling `:265`, `:294`; ticket QR `src/app/ticket/[token]/page.tsx:81`. Channel gate `src/lib/show-ops/outbound.ts:5`; actual environment/gate state and delivery not verified. Camera depends on browser BarcodeDetector support (`src/components/show-ops/ticket-scanner.tsx:73`), with paste fallback. |
| Show comments/diet in same colours | supported-code | Door flags `src/app/dashboard/show-ops/door/page.tsx:236`; shared colours `src/components/show-ops/booking-flags.tsx:32`. |
| Remove Day, just evening / show everyone for night | supported-code | Door date query selects show_date without AM/PM filtering (`src/app/dashboard/show-ops/door/page.tsx:74`, `:77`). Thus all bookings on selected date are shown, not only evening bookings. |
| Need live trial; refresh every 15 sec for two phones | needs-input | Refresh implemented `src/components/show-ops/door-auto-refresh.tsx:13`, `:28`, with hidden-tab pause/resume. Real show-night and simultaneous two-phone acceptance remains unperformed, as source itself acknowledges. |
| Timestamps local device clock; Canary time hover | supported-code | `src/components/show-ops/local-time.tsx:20`, `:29`; Door uses component `src/app/dashboard/show-ops/door/page.tsx:223`. Incorrect device timezone will affect displayed local time. |
| Door and quick check-in mixed | supported-code | Night lists/door purpose labels above; both intentionally work on same arrival data. A user walkthrough remains useful but not code missing. |
| App version | partial | Standalone web-app manifest `src/app/manifest.ts:11` and metadata `src/app/layout.tsx:28`, `:34`. This is an installable web experience, not evidence of native App Store release or offline support. |
| Photo attach at door/lists; invoice print; sending being built | supported-code | Ticket photo `src/app/dashboard/show-ops/door/page.tsx:258`; shared photo control `src/components/show-ops/no-show-decision.tsx:92`, `:136`; invoice-photo validation/attachments `src/lib/show-ops/invoice-photos.ts:27`, `src/lib/show-ops/invoice-delivery.ts:37`. Email attachment implementation exists; future-tense note stale. No real photo email was sent. |

## New booking — source lines 445–501

| Source note / answer | Verdict | Evidence and limits |
|---|---|---|
| Partner/island-specific rounded deposits (TFS/ACE, not LPA) | needs-input | Current deposit calculation is percentage rounded to cents (`src/lib/show-ops/calc.ts:134`), not nearest 5/10 or per-head rounding. Exact rounding direction/unit/partner eligibility required. |
| Typeahead in all boxes; response limits it to hotel/pickup/partner | partial | Partner search `src/components/show-ops/booking-form.tsx:1197`; hotel `:1535`; pickup `:1640`. Island/show/channel are still dropdowns, as answer admits. Broad “all boxes” request is only partly implemented unless that narrowed scope is accepted. |
| References continue after last Lanzasoft number; real bookings loaded | partial | Numeric sequence migration derives numeric portion and uses max+1 with lock (`supabase/migrations/20260902120000_show_ops_bus_count_and_ref_series.sql:15`, `:23`). This supports continuing imported numeric refs; completeness of imports and actual last legacy number not verified. |
| Match current guest ticket PDF template | needs-input | Source requests an example. Existing ticket/print page `src/app/ticket/[token]/page.tsx` is not evidence that an unprovided source template is matched. |
| Send to Guest button, email and text | partial | Booking detail button/channel results `src/app/dashboard/show-ops/bookings/[id]/page.tsx:330`; sender handles email and mobile (`src/lib/notifications/show-ops-guest-ticket.ts:229`); WhatsApp-first/SMS fallback `:214`. Requires recipient contact details, live gate and configured providers (`src/lib/notifications/booking-sms.ts:27`). Not verified delivered. |
| Payments direct/cash/card/transfer, exports, UK pounds separate | partial | Methods `src/lib/show-ops/types.ts:14`; booking method selection `src/components/show-ops/booking-form.tsx:1320`. A method label is distinct from actually recording a payment. Reports separate islands, but UK payment-link/background-ticket currency gaps above prevent blanket end-to-end claim. |
| Transport Show label blank; all shows now 7pm doors | needs-input | Configured show time displayed in form (`src/components/show-ops/booking-form.tsx:1672`, `:1766`). Actual live start-time values and rate cards unverified. Public/email tickets still display AM/PM-derived show time, not configured time (`src/app/ticket/[token]/page.tsx:54`; `src/lib/notifications/show-ops-guest-ticket.ts:295`). |
| Stop name/pickup time in pickup box; show time underneath | supported-code | Pickup selector and display `src/components/show-ops/booking-form.tsx:1640`, `:1672`; saved summary uses configured show time `:780`. Values depend on stop/show configuration. |
| Private hotel/villa/Airbnb/friends-family + resort; Private PDC · Villa on lists, fuller resort on guest ticket | supported-code | Private fields `src/components/show-ops/booking-form.tsx:1564`, `:1586`; shared label/zone helper `src/lib/show-ops/private-pickup.ts`; guest private transfer wording `src/lib/notifications/show-ops-guest-ticket.ts:53`, `:87`, `:151`. No extra transport charge or operational service availability inferred. |

## Intro assertions affecting these sections

The PDF says “Done” means live and says outbound messages are off to real recipients until one switch is enabled. Code exposes a master live flag (`src/lib/show-ops/outbound.ts:5`) and gated test-email allowlist (`:9`), while SMS/WhatsApp additionally need provider configuration. This audit has not read live secrets/configuration or exercised delivery, so neither current off-state nor universal readiness after one switch is verified. The PDF's own caveat that no real show-night trial has occurred must remain visible until that operational trial is actually completed.


## Dashboard (source notes lines 99–139)

| Your source note | Verdict | evidence and what still needs checking |
|---|---|---|
| Remove FUE — Done | Needs input / current data verification | Dashboard renders configured islands, not a hard-coded exclusion (`src/app/dashboard/show-ops/page.tsx:267–272`). The generic seed still includes Fuerteventura (`src/lib/show-ops/config.ts:248`). Joel, I cannot confirm FUE is gone from your live configuration from this code alone. |
| Select month/year — Done | Supported code | Month input and preserved island filter exist (`page.tsx:245–255`), with month-bound query at 73–82. Joel, the selector exists, but the completeness of its totals needs the row-limit correction below. |
| Need attention — basic list now; personalised workflow extra | Partial | Attention items render at `page.tsx:385–416`; generated from dashboard helper input. No mark-done/clear/assigned-user workflow established. Joel, basic warnings exist; personalised workflow is outstanding. Whether separately priced must be reconciled with prior agreed scope, not inferred from code. |
| Custom per user; mark done, clear, notify seniors; user-based dashboard | Partial / needs input | Role/page gates exist (`src/lib/show-ops/nav.ts:140–146`); shared dashboard has no persistent task completion/assignment interface (`page.tsx:385–416`). Joel, access differs by person; a personalised action queue is not delivered by those permissions. |
| Reservation users see full show/bus and cannot overbook; staff override | Supported code, not live-tested here | Existing capacity enforcement is in booking validation and seller DB triggers; audited in the companion partner report. Joel, keep this separate from personalised notifications and cancellation approvals. |
| Last few seats triggers senior notification | Needs input / outstanding | Existing warning display is not a configurable threshold-notification workflow. Threshold, recipients and closure authority still need agreement; no live delivery verified. |
| Supplier cancellation approval “drafted, not switched on” | Partial | Seller portal instructs contact office (`src/app/partner/page.tsx:290`); no usable request/approval flow established. Joel, this is outstanding; “drafted” is not a delivered function. |
| Team messaging, can probably build in a day | Needs input | No team chat implementation established in Show Ops routes. Joel, do not treat the estimate or extra-package classification as verified delivery or an agreed quote. |

## Reports (source lines 503–537)

| Your source note | Verdict | evidence and what still needs checking |
|---|---|---|
| Certain users only — Done | Partial | Page uses `requireShowOpsPage('reports')` and commercial-module gate (`reports/page.tsx:56–59`). However daily-sales export checks authentication/workspace only, not Reports page permission (`src/app/api/show-ops/daily-sales.csv/route.ts:15–25`); underlying JWT RLS still applies. Joel, hiding Reports is not presently an equivalent export permission boundary. |
| More breakdowns; Tickets & Stats spreadsheet | Needs input | Existing breakdowns group partner, type, hotel, show and night (`reports/page.tsx:241–275`). Joel, we still need your sheets and definitions to reconcile each breakdown. |
| Better PDF / Google Sheets export, monthly packs | Partial | Booking and daily sales CSV endpoints exist; no reports PDF/Google Sheets connector/monthly pack generator established. CSV does not mean automatic synchronisation. Joel, layouts, frequency and recipients need agreement; pricing status is not a code fact. |
| Tour Op / Direct split not right | Needs input, supported concern | Headline split counts bookings by `sales_channel` (`reports/page.tsx:217–218,353–354`), while partner-type breakdown uses supplier `partner_type` (`205–210,247–259`). Joel, these are different classifications; we need to match your intended denominator and correct imported labels. |
| Remove Cash Taken — Done | Supported code | Explicit removal with card/Stripe tile retained (`reports/page.tsx:285–288`). |
| Stats removed from menu and folded into Reports | Supported code | Nav has Reports only (`nav.ts:44,103–105`); old stats route redirects with filters (`src/app/dashboard/show-ops/stats/page.tsx:1–20`); year RPC and block at `reports/page.tsx:178–203`. |
| Daily sales now in Reports | Supported code with completeness caveat | Daily window uses office-local creation time (`reports/page.tsx:72–75,162–187`), not show date. Joel, this is bookings taken that day, not necessarily performances that day. |
| Week/month/year with island selector, custom sheets still needed | Supported code / needs input | Range helper called at `reports/page.tsx:61–69`, period controls supplied by `report-range.ts`; same selected dates applied to show-date bookings. Joel, the preset filters exist; your side-by-side island columns and workbook reconciliation remain outstanding. |
| Full-year totals / complete reporting implied in introduction | False as an unconditional completeness claim | Reports makes single `.range(0,9999)` requests (`reports/page.tsx:121–177,333–339`), not pagination. A server row cap can truncate earlier, and rows beyond requested range are omitted. No exact-count comparison. Query errors are discarded by destructuring only data (`111–120`) and converted to empty/zero (`187–194,214–216`). Joel, totals can be incomplete or show zero after a failed query; we must fix this before claiming full-year accuracy. |
| CSV exports | Partial | Bookings CSV uses `.limit(5000)` (`bookings.csv/route.ts:39–46`), daily CSV single `.range(0,9999)` (`daily-sales.csv/route.ts:32–50`). Neither paginates or detects omitted rows; both do surface actual query errors. Joel, downloadable does not yet guarantee every matching row is included. |
| UK reports separately in pounds / money totals | Partial; mixed totals incorrect | Selected-island report formatting uses island currency (`reports/page.tsx:306`), but all-island booking sums combine numeric values with no currency grouping (`214–216`). Invoices/payments ignore selected island (`142–160,282–288`). Year block also formats a combined number using one currency (`203`). Dashboard always uses workspace currency even when UK selected (`page.tsx:149,213`). Joel, EUR and GBP must be shown separately; these summaries are not a currency conversion and must not be added together. |
| CPH buses already in Reports | Supported calculation / needs formula agreement | Cost per head = each bus order's cost divided by booked transport passengers for that date/island (`reports/page.tsx:343–350`). Includes infants through `paxTotal`, uses selected partner-filtered bookings, and is not actual boarded passengers or per individual vehicle. Joel, send your sheet so the denominator and multi-bus treatment match your practice. |

## Finance / invoices (source lines 575–621)

| Your source note | Verdict | evidence and what still needs checking |
|---|---|---|
| Month end one click, every partner gets draft, review/issue/send/pay/chase | Partial | Draft pack generation reads uninvoiced invoice bookings (`actions.ts:2522–2537`), excludes imported `legacy_status='Invoiced'` (`2529`), creates draft (`2602–2618`). This query is unpaginated. Joel, the workflow exists, but “every” eligible booking/partner and a real period reconciliation still require verification; generation is not automatic issuing/sending. |
| Ticket pictures in invoice send “Building/not switched on” | Outdated: supported code now | Send loads invoice + scoped evidence, requires reviewed fingerprint and missing-photo acknowledgement, then attaches PDF and originals (`actions.ts:3245–3252,3284–3285`). Photo associations and paths validated (`invoice-photos.ts:27–40`), private downloads fail honestly (`49–63`). Joel, original photo attachments are implemented; one combined photo PDF is not what this code sends. Actual inbox receipt remains unverified. |
| All ticket photos delivered | Partial with explicit limits | Maximum 50 photos, each 5 MiB, total PDF+photos 18 MiB (`invoice-photos.ts:7–9,43–54`; `invoice-delivery.ts:38`). Missing photos require acknowledgement; invalid/unreadable photos block send. Joel, large packs need splitting, and the review makes missing evidence visible rather than silently claiming completeness. |
| Packs preview zero fixed; only IGIC explains difference | Partial / claim too strong | Preview uses stored netts and no-show billing (`invoices/page.tsx:94–139`) but generation may recompute no-snapshot rows using live rates (`actions.ts:2559–2585`). Preview does not apply generation's legacy-Invoiced exclusion (compare preview 100–107 with actions 2529). Both queries are unpaginated. Joel, differences can be caused by historical pricing/import exclusions or incomplete results, not only tax. Reconcile one actual period. |
| Invoice PDF has logo, both NIFs, lines, net/tax/total, paid stamp | Partial | PDF renders both legal-party blocks and NIFs if present, line quantity/net/tax/total and paid status (`invoice-pdf.ts:83–106`). Renderer contains no image/logo embedding: “your logo” is unsupported for this downloadable attachment. Joel, legal data must also be populated; the template cannot supply missing NIFs. |
| PDF download and email attachment done; inbox untested | Supported code; live verification still required | Issued/non-voided invoice required (`invoice-delivery.ts:9–20`), stored invoice rendered once for attachment. Sending uses PDF and optional photos (`actions.ts:3284–3285`). Gate/provider failures throw, and failed status logging explicitly warns email may have sent (`3296–3303`). Joel, a controlled recipient test and your layout review remain required; provider acceptance is not proof of inbox delivery. |
| Sent time/recipient recorded; opened tracking to agree | Supported code / tracking outstanding | `emailed_at` and `emailed_to` recorded after provider success (`actions.ts:3298–3303`). No per-invoice opened/read confirmation identified. Joel, recorded send is not an opened/read receipt. |
| Overdue list biggest first, each opens invoice | Supported code with limits | List sorts nominal total descending and links invoice (`invoices/page.tsx:541–554`); query at 61–68 is unpaginated and does not restrict status to issued. Joel, check whether drafts should appear, and mixed-currency ordering is nominal amount, not converted economic size. |
| Morning email lists every overdue invoice and count | Partial | Daily loader queries overdue invoices (`daily-report.ts:79–87`) with currency per row, but no pagination and query errors discarded (`48–49`). Joel, “every” is not guaranteed, and actual scheduled sends/recipient gates were not verified here. |
| Verifactu | Needs input / provider verification | Configurable adapter/manual mode exists (`src/lib/show-ops/verifactu.ts:8–29`). Joel, Jesus/provider validation is still needed. Code fields and email wording are not tax approval or proof of accepted submissions. |
| Invoice currency | Partial; guard needed | Pack currency comes from single island if unique, otherwise selected island or default (`actions.ts:2595–2618`). No mixed-currency rejection established in this generation path. PDF honours stored currency (`invoice-pdf.ts:13,64`), which cannot repair a mixed pack. Joel, separate GBP and EUR packs before creation and prevent mixed-currency packs in code. |
| Dashboard money/attention totals | Partial | Month/week rows, unpaid deposits and invoices unpaginated; errors ignored (`page.tsx:73–134`). Deposit and invoice queries do not follow island/month filters. Joel, some cards are workspace-wide while others follow the month/island selector; labels and aggregation need reconciliation, not a blanket “all totals correct.” |

## Accounts / P&L and existing sheets (source lines 623–735)

| Your source note | Verdict | evidence and what still needs checking |
|---|---|---|
| Accounts seniors only; island/combined P&L; invoice income with adjustments; manual income; categories; expenses; receipt files; cash box; outstanding outs and notifications; accountant downloads | Not implemented in Show Ops / needs input | Show Ops routes include payments and invoices but no accounts ledger, expenses/P&L, payables or receipt-entry module. `src/app/account/page.tsx` is account management, not this ledger. Joel, these are future requirements; access rules, accounting categories and file formats need agreement. “Priced separately” must be checked against commercial commitments, not asserted from code absence. |
| IGIC split already on invoices; P&L tax split and Jesus approval | Supported invoice fields / needs input for accounting | Invoice PDF line net/tax/total at `invoice-pdf.ts:104–106`; invoice configuration used at `actions.ts:2598–2599`. Joel, invoice arithmetic is not an accounting/tax sign-off; P&L treatment and filings remain with Jesus. |
| Payroll, card reconciliation, Golden Ticket, in-resort UK tracker, Harper tracker | Needs input / not established | No corresponding Show Ops modules established by route search. Joel, provide the source trackers/statements and intended users before scope, timing or price is promised. |
| Sales sheets ×3 islands; suppliers; P&L sheets | Needs input | Existing report structure described above is not evidence of matching the supplied workbook. Partner record counts are historical/unverified here. Joel, reconcile against the actual source sheets rather than assuming imported counts prove accuracy. |
| Lanzasoft current year imported; remaining cutover/older years | Needs data verification | Code recognises legacy bookings and avoids regenerating already-invoiced records (`actions.ts:2529`); this does not prove import completeness or cutover reconciliation. Joel, dates, totals and opening balances need explicit source-to-hub checks. |
| Outstanding invoices already there; opening unpaid balances at cutover | Partial / future import | Overdue view exists, but no completed opening-balance import verified. Joel, existing invoice screens are not proof all historical debts have been loaded. |
| Outstanding outs | Needs input / outstanding | Payables is part of the unimplemented accounting workflow, distinct from invoice receivables. |

## Immediate corrections before describing these notes as “Done”

Joel, invoice originals can now travel with the PDF, but inbox delivery and your layout approval still need checking. Reports and dashboard summaries need complete-result loading, visible failures, consistent filter scope and separate currencies. Invoice preview/generation need the same eligibility rules and safeguards against incomplete or mixed-currency packs. The attached PDF does not currently include your logo. Accounts/P&L and personalised task workflows remain requirements, and their commercial treatment must follow what was already agreed with you.

## Outlook (source lines 141–163)

| Your source note | Verdict | evidence and what still needs checking |
|---|---|---|
| Move under Show calendar | Supported code | Calendar link in Outlook header and each night (`src/app/dashboard/show-ops/outlook/page.tsx:272–275,412–415`); planning navigation groups these pages in `src/lib/show-ops/nav.ts`. Joel, the screens link together. |
| Remove FUE, off island list everywhere | Partial | Outlook static sections omit FUE (`outlook/page.tsx:33–61`). “Everywhere” depends on current workspace configuration; dashboard is config-driven (`dashboard/show-ops/page.tsx:270`). |
| Tenerife merge TFS & TFW; remove Puerto de la Cruz | Supported code with wording correction | One Tenerife section has South/Adeje/Golf/West columns (`outlook/page.tsx:52–58`). Comment at 27–31 explicitly says remaining Puerto bookings appear in Other. Joel, its separate column is removed; historical bookings are not deleted. |
| €€ means booked total and average, not door money | Supported concept / currency defect | Adds each booking total into night value (`outlook/page.tsx:243`); currency function hardcodes euros (`101–103`), also applied to UK section and rates (`324,384,440`). Joel, it is booking value, but UK must display pounds; this is not a conversion or cash-to-collect figure. |
| Bus count supports 2 buses and seats | Supported code | Sums bus_count and seats by date/island (`outlook/page.tsx:162–170`), displays plural bus count (`446`). This is ordered buses, not automatically calculated required vehicles. |
| 7 or 14 nights, defaults 7 | Partial — off-by-one defect | Selector defaults 7 (`outlook/page.tsx:114–116`), but end=today+days (`128`) and date loop is inclusive (`211`), so it constructs 8/15 calendar dates. Joel, selector wording exists but its actual range needs correction to 7/14 nights. |
| Hotel / Resort toggle | Supported code | `by` selects hotel/resort (`outlook/page.tsx:111–124`); resort roll-up sums hotel pax/value (`85–98`). Joel, these are two groupings of the same bookings. |
| Outlook completeness (cross-cutting) | Partial | All four queries unpaginated and query errors discarded (`outlook/page.tsx:130–156`). Joel, row caps or failed reads can omit bookings/stops and distort totals; fix alongside Reports and Dashboard. |




## verification for this change

185 Show Ops application tests passed. The dedicated recursion suite passed all 5 tests, reproducing the old failure first and checking the repaired role boundaries. All 13 island-permission tests passed, including owner precedence and switching users in a prepared booking query. TypeScript and targeted lint passed. The independent review found the leap-day comparison issue and it was corrected with a regression test. These checks do not replace live email, payment, restore or show-night acceptance
